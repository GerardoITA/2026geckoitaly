namespace = ruler_failsafe


############################################################
# OTTOMAN FAILSAFE
############################################################

country_event = {
	id = ruler_failsafe.1
	title = ruler_failsafe.1.t
	desc = ruler_failsafe.1.d
	picture = COURT_eventPicture
	is_triggered_only = yes

	immediate = {
		set_country_flag = ruler_failsafe_ottomans_used

		define_ruler = {
			name = "Dehmem"
			dynasty = "Osmanoglu"
			culture = turkish
			adm = 5
			dip = 4
			mil = 5
			male = yes
			age = 40
		}
	}

	option = {
		name = ruler_failsafe.1.a
	}
}


############################################################
# POLAND FAILSAFE
############################################################

country_event = {
	id = ruler_failsafe.2
	title = ruler_failsafe.2.t
	desc = ruler_failsafe.2.d
	picture = COURT_eventPicture
	is_triggered_only = yes

	trigger = {
		tag = POL
		NOT = { is_year = 1470 }
		has_country_flag = pol_denied_pu_flag
		NOT = {
			has_country_flag = ruler_failsafe_poland_used
		}
	}

	immediate = {
		set_country_flag = ruler_failsafe_poland_used

		define_ruler = {
			name = "Polbocs"
			culture = polish
			adm = 5
			dip = 5
			mil = 5
			male = yes
			age = 35
		}
	}

	option = {
		name = ruler_failsafe.2.a
	}
}