namespace = eyalet_colors

country_event = {
	id = eyalet_colors.1
	title = eyalet_colors.1.t
	desc = eyalet_colors.1.d
	picture = COURT_eventPicture
	is_triggered_only = yes

	option = {
		name = eyalet_colors.1.a

		every_subject_country = {
			limit = {
				OR = {
					is_subject_of_type = eyalet
					is_subject_of_type = core_eyalet
				}
			}

			change_country_color = {
				country = TUR
			}
		}
	}

	option = {
		name = eyalet_colors.1.b

		every_subject_country = {
			limit = {
				OR = {
					is_subject_of_type = eyalet
					is_subject_of_type = core_eyalet
				}
			}

			restore_country_color = yes
		}
	}
}